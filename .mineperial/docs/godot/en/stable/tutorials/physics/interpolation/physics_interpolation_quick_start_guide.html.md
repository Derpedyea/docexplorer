**Quick start guide – Godot Engine (stable) documentation**

> *Turn on physics interpolation: Project Settings → Physics → Common → Physics Interpolation.  
> Make sure you move objects and run your game logic in `_physics_process()` rather than `_process()`. ...*

*(The full guide continues with step‑by‑step instructions on enabling physics interpolation, moving objects in `_physics_process()`, and testing the effect on gameplay. For the complete, up‑to‑date documentation, see the official Godot docs at <https://docs.godotengine.org/en/stable/tutorials/physics/interpolation/physics_interpolation_quick_start_guide.html>.)*