**User interface (UI)**  
*Godot Engine (stable) documentation*

In this section of the tutorial we explain the basics of creating a graphical user interface (GUI) in Godot.  
UI building blocks: Like everything else in Godot the user interface is built using node…  

---

## Table of contents

1. [Size and anchors](size_and_anchors.html)
2. … (other sub‑pages in the UI tutorial)

---

### Overview

- **What is UI in Godot?**  
  A brief introduction to how Godot handles graphical user interfaces, including the node‑based approach and the types of UI controls available.

- **UI building blocks**  
  An explanation of the fundamental nodes used to construct GUIs such as `Control`, `Button`, `Label`, `TextureRect`, etc.

- **Layout and anchors**  
  Guidance on positioning and scaling UI elements with anchors, margins, and different layout containers (`VBoxContainer`, `HBoxContainer`, `GridContainer`, etc.).

- **Theme and styling**  
  How to customize the look and feel of UI controls using themes, theme overrides, and CSS‑style properties.

- **Handling input**  
  Using signals and event handling to respond to user interactions like clicks, drags, and text input.

---

### Suggested reading

- **Size and anchors** – Learn how to make UI elements responsive to different screen sizes.  
- **Layout containers** – Understand the built‑in containers for arranging child controls automatically.  
- **Themes** – Discover how to create and apply custom themes to your project.  
- **Signals in UI** – Connect UI signals to script logic for interactive behaviour.

---