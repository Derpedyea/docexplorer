**Support different actor types**

> *To support different actor types due to e.g. their sizes each type requires its own navigation map and navigation mesh baked with an appropriate agent radius and height. The same approach can be used …*  

*(Note: The full tutorial content is not included in the provided HTML snippet.)*