**File system**  
*Godot Engine (stable) documentation*

---

### Introduction  

A file system manages how assets are stored and how they are accessed. A well‑designed file system also allows multiple developers to edit the same source files and assets while collaborating, and it enables efficient resource loading and caching.

---

*(The full tutorial covers the structure of a Godot project directory, how the editor’s `FileSystem` dock works, best practices for organizing files, and how to use the `ResourceLoader`, `File`, and `Directory` APIs to load and manipulate resources at runtime.)*