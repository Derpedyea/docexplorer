**EditorFileDialog** – Godot Engine (stable) documentation
============================================================

> *Inherited from:*  
> `ConfirmationDialog` → `AcceptDialog` → `Window` → `Viewport` → `Node` → `Object`

Description
------------

`EditorFileDialog` is a specialized version of `FileDialog` that is used internally by the Godot editor. It provides additional features and callbacks tailored for editor workflows, such as integration with the project file system and editor‑specific file filters.

> *Source:* <https://docs.godotengine.org/en/stable/classes/class_editorfiledialog.html>

> (The full class reference – signals, methods, properties – can be found in the official Godot documentation.)

> **Note**: This page is a part of the Godot class reference. It contains technical details such as method signatures, signal definitions, and inheritance hierarchy, which are essential for developers who need to subclass or interact programmatically with `EditorFileDialog`.