**Note:** The provided HTML snippet does not contain the detailed contents of the `GPUParticles3D` class page (properties, methods, signals, etc.). The following Markdown is a structured skeleton that reflects the typical layout of a Godot class reference page, based on the visible portion of the HTML and the standard structure of the Godot documentation.  

Feel free to replace the placeholder sections with the actual content if it is available elsewhere in the original page.

---

# GPUParticles3D

*Inherited from:*  
`GeometryInstance3D` → `VisualInstance3D` → `Node3D` → `Node` → `Object`

## Description

A 3D particle emitter node used to create a variety of particle systems and effects.

## Signals

| Signal | Description |
|--------|-------------|
| | |

*(Replace with actual signal names and descriptions.)*

## Properties

| Property | Type | Default | Description |
|----------|------|---------|-------------|
| | | | |

*(Replace with actual property names, types, defaults, and descriptions.)*

## Methods

| Method | Signature | Description |
|--------|-----------|-------------|
| | | |

*(Replace with actual method names, signatures, and descriptions.)*

## Constants

| Constant | Value | Description |
|----------|-------|-------------|
| | | |

*(Replace with actual constants.)*

## Enums

### `EmissionShape` (example)

| Value | Meaning |
|-------|---------|
| `EMISSION_SHAPE_POINT` | Emits from a single point |
| `EMISSION_SHAPE_SPHERE` | Emits from a sphere |
| `EMISSION_SHAPE_BOX` | Emits from a box |
| `EMISSION_SHAPE_CYLINDER` | Emits from a cylinder |
| `EMISSION_SHAPE_PARTICLES` | Emits from a pre‑defined shape |

*(Add other enums as defined in the class.)*

## Example Usage

```gdscript
extends GPUParticles3D

func _ready():
    amount = 500
    lifetime = 2.0
    # ... configure other properties ...
```

## See Also

- [`GPUParticles2D`](../class_gpuparticles2d.html)
- [`GPUParticlesAttractor3D`](../class_gpuparticlesattractor3d.html)

---

*(End of skeleton.)*